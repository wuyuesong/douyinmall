syntax = "proto3";

package frontend.image;

option go_package = "frontend/image";

import "api.proto";
import "frontend/common.proto";

service ImageService {
   rpc Image(common.Empty) returns(common.Empty) {
      option (api.get) = "/image/:image";
   }
}
