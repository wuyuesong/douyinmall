// idl/hello/hello.proto
syntax = "proto3";

package frontend.admin;

option go_package = "frontend/admin";

import "api.proto";
import "frontend/common.proto";

service HomeService {
   rpc Home(common.Empty) returns(common.Empty) {
      option (api.get) = "/admin/home";
   }
}
